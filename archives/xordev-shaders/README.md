# Archived XorDev-derived shaders

These shaders were removed from the active sketch registry and are intentionally not loaded by the live app.

Keep them here only as an internal archive so they can be restored later if explicit permission is granted.
